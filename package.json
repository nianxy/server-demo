{
  "name": "server-demo",
  "version": "1.1.0",
  "description": "HTTP demo server with Express",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "migrate": "db-migrate up",
    "migrate:reset": "db-migrate reset"
  },
  "dependencies": {
    "express": "^4.18.2",
    "pg": "^8.11.3",
    "ioredis": "^5.4.1"
  },
  "devDependencies": {
    "db-migrate": "^0.11.14",
    "db-migrate-pg": "^1.5.2"
  }
}
